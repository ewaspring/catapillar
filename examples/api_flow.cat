# API 行为流示例：根据 URL 调用接口，并按状态分支处理

# 用户输入 URL
~ 输入 -> 解析URL

# 解析后的 URL 交给请求器
~ 解析URL -> 调用接口

# 调用接口后，根据结果分支
! 调用接口 -> 处理成功
? 调用接口 -> 处理失败


# ---- 成功分支 ----
~ 成功流程:
    响应体 -> 提取数据
    提取数据 -> 输出成功
end


# ---- 失败分支 ----
~ 失败流程:
    响应体 -> 输出错误
end
